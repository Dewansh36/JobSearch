import React, { Component } from "react";
import { Grid } from "@mui/material";
const Home = () => {
    return (
        <Grid container spacing={3}>
            <h1>Cards</h1>
        </Grid>
    )
};

export default Home;